# Action Targets

Small sandbox-template fixture that proves the default `start` target and named `actions`.

```bash
openpond sandbox-template run --file openpond.yaml
openpond sandbox-template run --file openpond.yaml --target summarize
openpond sandbox-template run --file openpond.yaml --action export-json
```

