# Service With Actions

Small sandbox-template fixture that proves a long-lived service plus one-off actions.

```bash
openpond sandbox-template run --file openpond.yaml --target ingest
openpond sandbox-template run --file openpond.yaml --target snapshot
openpond sandbox-template dev --file openpond.yaml
```

