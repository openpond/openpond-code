# Preview Service

Small sandbox-template fixture that proves a long-lived service with a preview port.

```bash
openpond sandbox-template run --file openpond.yaml
openpond sandbox-template dev --file openpond.yaml
```

The local preview URL is `http://127.0.0.1:3000/`.

