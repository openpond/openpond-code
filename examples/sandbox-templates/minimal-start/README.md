# Minimal Start

Small sandbox-template fixture that proves a default `start` command can run and write an artifact.

```bash
openpond sandbox-template validate --file openpond.yaml
openpond sandbox-template build --file openpond.yaml
openpond sandbox-template run --file openpond.yaml
```

