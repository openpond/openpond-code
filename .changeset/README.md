# Changesets

This project uses [Changesets](https://github.com/changesets/changesets) to manage versions and changelogs.

## Common commands

- `npm run changeset` to create a new changeset.
- `npm run changeset:version` to apply pending changesets.
- `npm run changeset:publish` to publish to npm.
