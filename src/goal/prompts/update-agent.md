# Update OpenPond Agent Goal

Update an existing source-backed OpenPond agent from the user's request and evidence refs. Run before/after checks through the openpond_agent_* tools and leave a reviewable source update.

Do not invoke openpond-agent through shell, npx, pnpm dlx, or yarn dlx.
